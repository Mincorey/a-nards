-- =============================================================================
-- A-NARDS · Онлайн-жеребьёвка «кто ходит первым».
-- -----------------------------------------------------------------------------
-- Раньше start-game просто случайно назначал цвет первого игрока. Теперь сервер
-- бросает по одной кости на цвет (перебрасывая при равенстве), у кого больше —
-- тот ходит первым, и сохраняет выпавшие кости в games.opening = { w, b }.
-- Оба клиента показывают одинаковые кубики жеребьёвки на своих сторонах доски.
-- Идемпотентно.
-- =============================================================================
alter table public.games
  add column if not exists opening jsonb;
